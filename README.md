# mini-lovable
AI website generator for premium landing pages

## Sandbox commands

- `npm run smoke-run` — repo-local MVP smoke loop: git status before, build, git diff --stat, git status after, plus a PASS/FAIL summary.
